<div class="panel-group">
  <%= f.fields_for :configuration, include_id: false do |conf| %>
    <%= collapsible_panel 'General', active: true do %>
      <%= render 'voice_in_trunks/fields/name_and_capacity_limit', f: f %>
      <%= render 'voice_in_trunks/fields/username_host_protocol_port', f: f, conf: conf %>
      <%= render 'voice_in_trunks/fields/resolve_ruri', f: f, conf: conf %>
      <%= render 'voice_in_trunks/fields/cli_format_prefix', f: f %>
    <% end %>

    <%= collapsible_panel 'Voice In Trunk group configuration' do %>
      <%= render 'voice_in_trunks/fields/trunk_group', f: f %>
      <%= render 'voice_in_trunks/fields/disconnect_codes', f: f, conf: conf %>
    <% end %>

    <%= collapsible_panel 'Authentication' do %>
      <%= render 'voice_in_trunks/fields/auth_enabled', f: f, conf: conf %>
      <%= render 'voice_in_trunks/fields/auth_user_pwd', f: f, conf: conf %>
      <%= render 'voice_in_trunks/fields/auth_from', f: f, conf: conf %>
    <% end %>

    <%= collapsible_panel 'Media & DTMF' do %>
      <%= render 'voice_in_trunks/fields/codec_ids', f: f, conf: conf %>
      <%= render 'voice_in_trunks/fields/dtmf_and_rtp', f: f, conf: conf %>
    <% end %>

    <%= collapsible_panel 'Advanced Signalling Settings' do %>
      <%= render 'voice_in_trunks/fields/advanced_signalling', f: f, conf: conf %>
    <% end %>

  <% end %>
</div>
