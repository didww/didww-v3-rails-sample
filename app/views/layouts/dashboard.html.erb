<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>
  <body>
    <div id="wrapper">
      <%= render 'layouts/dashboard/navigation' %>
      <!-- Page Content -->
      <div id="page-wrapper">
        <div class="js-flash-wrapper">
          <%= render 'layouts/messages' %>
        </div>
        <%= yield %>
      </div>
      <!-- /#page-wrapper -->
    </div>
  </body>
</html>
