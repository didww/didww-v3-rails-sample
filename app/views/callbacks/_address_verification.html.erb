Address Verification <%= link_to callback_payload[:id], export_path(callback_payload[:id]) %>
were <%= callback_payload[:status] %>
<%= "because of #{callback_payload[:reject_reasons]}" if callback_payload[:reject_reasons].present? %>
